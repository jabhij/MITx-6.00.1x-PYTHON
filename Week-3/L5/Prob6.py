def lenIter(aStr):
    '''
    aStr: a string
    
    returns: int, the length of aStr
    '''
    # Iterative Function
    count = 0
    
    for char in aStr:
       count += 1
       
    return count
